{
  "name": "atuona-gallery",
  "version": "1.0.0",
  "description": "ATUONA Gallery of Moments - Underground Verse Vault with REAL NFT Minting",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run vault:build",
    "build": "vite build",
    "preview": "vite preview",
    "vault:build": "node scripts/extract-poems.mjs && node scripts/build-vault.mjs && node scripts/build-dna.mjs && node scripts/build-words.mjs && node scripts/build-type.mjs && node scripts/verify-vault.mjs",
    "vault:verify": "node scripts/verify-vault.mjs",
    "vault:lock": "node scripts/verify-vault.mjs --lock",
    "vault:test": "node scripts/test-vault-guards.mjs",
    "verify": "node scripts/verify-vault.mjs && node scripts/test-vault-guards.mjs && node scripts/audit-against-baseline.mjs",
    "lazy-mint": "node automated-lazy-mint.js",
    "setup-nfts": "node automated-lazy-mint.js",
    "vault:audit": "node scripts/audit-against-baseline.mjs"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "thirdweb": "^5.105.41"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "sharp": "^0.34.3",
    "vite": "^5.4.19"
  }
}
