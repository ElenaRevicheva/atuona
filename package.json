{
  "name": "atuona-gallery",
  "version": "1.0.0",
  "description": "ATUONA Gallery of Moments - Underground Verse Vault with Direct Contract Calls",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "build": "mkdir -p dist && cp *.html *.css *.js dist/",
    "preview": "python3 -m http.server 8080 --directory dist"
  }
}